[package]
name = "kivo"
version = "0.2.0"
edition = "2021"

[dependencies]
serde        = { version = "1", features = ["derive"] }
bincode      = "1.3"
bytes        = "1.5"
tokio        = { version = "1", features = ["full"] }
clap         = { version = "4", features = ["derive"] }